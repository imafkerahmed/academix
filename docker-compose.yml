services:
  pocketbase:
    build: ./services/pocketbase
    container_name: pocketbase
    ports:
      - "8090:8090"
    volumes:
      - ./services/pocketbase/pb_data:/pb/pb_data
    restart: unless-stopped
    env_file:
      - .env.local
